import { escapeHtml, formatCurrency, getQuery, setStatus } from '../app/utils.js';
import { shareRepository } from '../repositories/repositories.js';
const root=document.querySelector('#share-content');const id=getQuery('id');const record=id?shareRepository.get(id):null;if(root){if(!record){root.innerHTML=`<div class="share-intro"><div class="section-kicker">Public design snapshot</div><h1>This link is<br>not available.</h1><p>It may be missing, revoked, or stored in another browser. No private account data is exposed.</p><a class="btn btn-light" href="/design/"><span>Begin your own design</span></a></div>`;}else{const labels=Object.values(record.selectionLabels||{}).filter(Boolean);root.innerHTML=`<div class="share-intro"><div class="section-kicker">Public design / ${escapeHtml(record.id.slice(-8).toUpperCase())}</div><h1>${escapeHtml(record.productName)}</h1><p>${escapeHtml(record.narrative||'A private design idea shared from the WOYKE atelier demonstration.')}</p>${!record.hidePrice?`<strong class="share-price">${formatCurrency(record.indicativeTotal)}</strong>`:'<p class="mono">GIFT MODE / PRICE HIDDEN</p>'}<div class="btn-row"><a class="btn btn-light" href="/design/?product=${encodeURIComponent(record.productSlug)}"><span>Create your version</span><span>+</span></a><button class="btn btn-secondary" id="copy-share" type="button"><span>Copy link</span></button></div><p id="share-status" class="prototype-note" hidden></p></div><article class="share-card"><div class="share-card-wordmark">WOYKE</div><img src="${escapeHtml(record.preview?.src)}" alt="${escapeHtml(record.preview?.alt||record.productName)}"><div class="share-card-copy"><span class="mono">SHARED DESIGN SNAPSHOT</span><h2>${escapeHtml(record.productName)}</h2><p>${escapeHtml(labels.slice(0,6).join(' / '))}</p><span class="mono">i am, we are.</span></div></article>`;root.querySelector('img')?.addEventListener('error',e=>{e.currentTarget.src='/assets/media/images/product-final.webp';},{once:true});const button=root.querySelector('#copy-share');const status=root.querySelector('#share-status');button?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(location.href);setStatus(status,'Public design link copied.');}catch{setStatus(status,location.href);}});}}