import { escapeHtml, formatCurrency, formatDate } from '../app/utils.js';
import { cartTotals } from '../domain/configuration.js';
import { cartRepository, designRepository, orderRepository } from '../repositories/repositories.js';
import { authService } from '../services/services.js';

const session=authService.requireSession(); const root=document.querySelector('#account-content');
if(session&&root){const designs=designRepository.list(session.userId);const orders=orderRepository.list(session.userId);const cart=cartRepository.list();const spend=orders.reduce((sum,order)=>sum+Number(order.totals?.total||0),0);root.innerHTML=`
<section class="account-hero" data-header-theme="light"><div class="container account-hero-grid"><div><div class="section-kicker">Private account / local demo</div><h1 class="display-lg">Good to see you.</h1><p class="lede">${escapeHtml(session.displayName)} / ${escapeHtml(session.email)}</p></div><div class="account-monogram">${escapeHtml(session.displayName.split(' ').map(x=>x[0]).join('').slice(0,2).toUpperCase())}</div></div></section>
<section class="scene scene-cream" data-header-theme="light"><div class="container account-dashboard"><a class="account-feature" href="/account/orders/"><span class="mono">DEMO ORDERS / ${orders.length}</span><h2>${orders[0]?escapeHtml(orders[0].reference):'No orders yet.'}</h2><p>${orders[0]?`Created ${escapeHtml(formatDate(orders[0].createdAt))}`:'Completed local orders will appear here.'}</p><span>View orders +</span></a><a class="account-feature" href="/account/designs/"><span class="mono">SAVED DESIGNS / ${designs.length}</span><h2>${designs.length?`${designs.length} ${designs.length===1?'idea':'ideas'} waiting.`:'Nothing saved yet.'}</h2><p>Resume, duplicate, share or remove a design.</p><span>Open designs +</span></a><a class="account-feature" href="/cart/"><span class="mono">SHOPPING BAG / ${cart.length}</span><h2>${formatCurrency(cartTotals(cart).total)}</h2><p>Configured lines remain in this browser.</p><span>Open bag +</span></a></div></section>
<section class="scene scene-ivory" data-header-theme="light"><div class="container"><div class="account-metrics"><div><span class="mono">DEMO ORDER VALUE</span><strong>${formatCurrency(spend)}</strong></div><div><span class="mono">SAVED DESIGNS</span><strong>${designs.length}</strong></div><div><span class="mono">ORDERS</span><strong>${orders.length}</strong></div></div><button id="account-signout" class="btn btn-secondary" type="button"><span>Sign out of demo</span></button></div></section>`;root.querySelector('#account-signout').addEventListener('click',()=>{authService.signOut();location.replace('/');});}